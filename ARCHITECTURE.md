# Camnemi 데이터 업그레이드 서비스 — 통합 아키텍처 설계

> 설계 원칙: **"싸게 추출하고, 싸게 검증하고, 드물게 승격한다"**
> (Extract cheap → Verify cheap → Escalate rarely)

작성 2026-09-12. 이번 세션에서 실제로 겪은 실패(환각·OCR실패·포맷깨짐·토큰만료·보강필드 손실)를 전부 반영.

---

## 0. 핵심 진단 — 왜 "파서·KB 성능 편차"가 생기는가

| 증상 | 실제 원인 (이번에 규명) | 해결 방향 |
|---|---|---|
| 파싱 결과가 학교마다 들쭉날쭉 | **소스 문서 품질** 차이 (텍스트 76k자 vs 306자) | 모델이 아니라 **문서 등급 라우팅** |
| 없는 값(period)이 생김 | 저텍스트 PDF에 LLM 투입 → **환각** | 텍스트 길이 **게이트** + "없으면 null" 프롬프트 |
| OCR해도 안 나옴 | 원본이 **웹 스크린샷**(요강 아님) | 수집 단계에서 **파일 유형 검증** |
| pro 파싱 크래시/빈 응답 | **reasoning 모델** → max_tokens 소진, content=None | 모델별 **호출 규약** 분리 |
| 배치 중 401 | 토큰 **1시간 만료** | 런타임 토큰 사용 + **재시도** |
| KB diff 폭증 | 포맷(indent/CRLF) 불일치 | **포맷 계약** 명시 |
| consulting_db 손실 | 리빌드가 보강필드 삭제 | **파생물은 in-place fill-only** |

**결론: 편차의 주범은 모델이 아니라 "소스 품질 × 검증 부재"다.** 모델 교체로는 안 고쳐진다.

---

## 1. 파이프라인 6단계 (역할 분리)

```
① 수집(Collect) → ② 분류(Classify) → ③ 추출(Extract) → ④ 검증(Verify) → ⑤ 병합(Merge) → ⑥ 배포(Publish)
   결정적            결정적             모델 사용          결정적+모델        결정적          결정적
   ($0)              ($0)               (유일한 과금)      (거의 $0)         ($0)           ($0)
```

**핵심: 과금은 3·4단계에서만 발생하고, 그마저도 "필요한 문서만".**

---

## 2. 문서 등급 라우팅 (성능 편차 해결의 핵심)

파싱 전에 **문서를 등급화**해서 처리기를 정한다. (실측 근거: rich 364 / medium 164 / poor 67 / image 6)

| 등급 | 판정 기준 | 처리기 | 모델 | 비고 |
|---|---|---|---|---|
| **A. 텍스트 풍부** | pymupdf ≥ 5,000자 | 직접 파싱 | **flash** (충분) | majors ~100% 정확 |
| **B. 텍스트 보통** | 1,000–5,000자 | 직접 파싱 | **flash** → 검증 실패 시 **pro** | |
| **C. 저텍스트** | 200–1,000자 | **OCR(rapidocr)** → 파싱 | **pro** | OCR 후에도 짧으면 null |
| **D. 이미지** | < 200자 | **OCR** → 파싱 | **pro** | OCR 결과 <30자면 **폐기** |
| **E. 비(非)문서** | URL/스크린샷 감지 | **재수집** (요강 아님) | — | 수집 단계로 반려 |

> ⚠️ **E등급 판정 규칙**: 파일에 `렌더`/`캡처`, 또는 URL·jsessionid·"Copyright"이 본문 대부분이면 문서 아님 → **재수집 큐**로.

---

## 3. 모델 사다리 (사용 가능 41개 중 선별)

| Tier | 역할 | 모델 | 근거 |
|---|---|---|---|
| **T0** | 결정적 추출 | pymupdf·regex·rapidocr·validator | **$0**, 재현 가능 |
| **T1** | 대량 구조 추출 (A/B등급) | **deepseek-v4-flash-0731** (현행) · `gemini-3.7-flash` · `glm-5.3-flash` | 저가·고속, 깨끗한 텍스트에 충분 |
| **T2** | 난도 추출 (C/D등급·모호) | **deepseek-v4-pro** (현행) · `claude-sonnet-5` · `gemini-3.1-pro-preview` | 추론형, 애매한 표·조건 해석 |
| **T3** | 심판 (분쟁·저신뢰) | `claude-opus-5` · `gpt-6-astra-pro` | 극소수만, 검증 불일치 조정 |
| **V** | 비전 OCR 보조 | `gemini-3.1-pro-preview` (이미지 입력) | rapidocr 실패 시 |

**운영 원칙**: 평소 **T1 90% · T2 10% · T3 1% 미만**. T3는 "T1과 T2 결과가 충돌"할 때만.

---

## 4. 신뢰도 점수 & 자동 승격 (escalation)

각 추출 레코드에 **결정적 검증 점수**를 매기고, 낮으면 자동 승격한다.

```
score = 0
+ 0.4  소스 등급(A/B/C/D)
+ 0.3  스키마 검증 통과율 (필드 형식·범위)
+ 0.2  원문 대조 (추출값이 PDF 텍스트에 실제 존재)   ← 이번에 구현한 validator
+ 0.1  교차 일치 (기존 KB·2차 파서와 일치)
```

| score | 조치 |
|---|---|
| ≥ 0.8 | ✅ 그대로 병합 |
| 0.5–0.8 | ⚠️ **T2로 재파싱** 후 병합 |
| < 0.5 | ❌ **T3/T2 + 사람 검토 큐** (자동 병합 금지) |

> 이번에 쓴 `_validate_llmparse.py`(원문 대조)가 바로 `+0.2` 항목의 구현체.

---

## 5. 스키마 계약 (파서·KB 양쪽 동일)

파서 출력과 KB 필드를 **한 스키마**로 고정 → 성능 편차가 "형식 오류"로 새는 걸 차단.

```jsonc
{
  "school": "건국대학교",
  "level": "ba|ma|lang|junior",          // ★ 필수 명시 (오분류 방지)
  "year": 2026,                           // 정수, 추정 금지
  "period": {"raw": "...", "from": "2026-09-07", "to": "2026-09-30"},
  "requirements": {                       // ★ level=lang 이면 항상 null
    "topik": 3, "ielts": 5.5, "toefl": 71,
    "selftest": false, "kiip": null, "note": "..."
  },
  "majors": ["..."],
  "tuition": {"note": "..."},
  "scholarship": {"note": "..."},
  "_meta": {"source_pdf": "...", "text_len": 76045, "tier": "A",
            "model": "deepseek-v4-flash-0731", "prompt_ver": "v3",
            "validator_score": 0.9, "parsed_at": "2026-09-12"}
}
```

**불변 규칙** (이번에 어긴 것들):
1. `level=lang` → `requirements` **항상 null** (어학연수는 TOPIK/IELTS 불요)
2. 원문에 없는 값 → `null` (추측 금지)
3. `_meta` 필수 (모델·프롬프트 버전·등급 = 재현성)

---

## 6. KB 계층 & 쓰기 규칙

```
verified_kb.json   (정본/SOT)      ← 파서 결과가 여기로만 병합
  ├─ kb_smart.json    (파생: 자격·택소노미)   ← 재생성 OK
  ├─ consulting_db.json (파생: 봇 조회)       ← ⚠️ in-place fill-only (보강필드 364+348)
  ├─ data.js          (파생: 홈페이지)
  └─ Supabase         (파생: 웹서비스)
```

| 규칙 | 내용 |
|---|---|
| **쓰기 순서** | 소스 → 정본 → 파생 (역방향 금지) |
| **파생물 갱신** | 재생성 스크립트 or **in-place fill-only** (보강필드 보존) |
| **포맷 계약** | verified_kb=indent1/LF · consulting_db=indent1/LF · data.js=minified |
| **백업** | 모든 정본 쓰기 전 `*_bak_날짜.json` |
| **충돌 방지** | 파생물은 **merge_global처럼 상위우선 override** 규칙 문서화 |

---

## 7. 일일 오케스트레이션 (크론 3단)

| 시간 | Job | 내용 | 모델 |
|---|---|---|---|
| **매일 08:00** | 수집·diff | adiga 스크레이프 + 홈페이지 203곳 검수 → **신규/변경만** 큐 | flash |
| **변경 감지 시** | 파싱·검증 | 등급 라우팅 → T1/T2 → validator 점수 | flash/pro |
| **평일 09:00** | 병합·배포 | 소스→정본→파생, 품질리포트, 커밋 | flash |

**멱등성**: 모든 단계는 **md5 해시 기반 skip** (이미 처리한 PDF는 재파싱 안 함).
**재개성**: 실패분은 큐에 남아 다음 틱에서 재시도 (이번 OCR 재파싱처럼).

---

## 8. 품질 게이트 (배포 전 필수)

1. **스키마 검증** — 모든 레코드가 §5 스키마 통과
2. **불변식 검사** — lang에 req 없음 / null 규칙 / level 유효
3. **회귀 테스트 (Golden Set)** — 고정 표본 50개로 **모델·프롬프트 변경 시 정확도 비교**
4. **이상치 탐지** — 등록금·요건 값이 분포 밖이면 경고
5. **diff 규모 상한** — 정본 diff가 임계 초과 시 중단 (포맷 사고 방지)

> 이번 세션의 `kb_quality_check.py` + `_validate_llmparse.py`가 1·2·4의 구현체. **3번(Golden Set)은 신규 필요.**

---

## 9. 모델 선택 가이드 (실전 규칙)

| 상황 | 선택 |
|---|---|
| 깨끗한 텍스트 대량 추출 | **flash** (T1) |
| 표·조건이 복잡 / OCR 텍스트 | **pro** (T2) |
| 추출값이 원문과 불일치 | **pro 재파싱** → 그래도 안 되면 **opus/astra** (T3) |
| 순수 요약·리포트 | **flash** |
| 이미지 판독 | rapidocr → 실패 시 **gemini-3.1-pro**(비전) |
| **절대 금지** | 저텍스트 문서에 pro 단독 투입 (환각) |

**비용 감각(실측)**: flash ≈ 30일 대량사용 $29 / pro 파싱 1건 ≈ $0.0035 upstream.

---

## 10. 지금 당장 적용할 5가지 (우선순위)

1. **문서 등급 라우터** 구현 (§2) — 파서 앞단에 tier 태깅
2. **검증 점수 + 자동 승격** (§4) — 저신뢰만 pro로
3. **E등급(스크린샷) 반려 룰** — 수집 단계에서 걸러 재수집 큐로
4. **Golden Set 50개** 구축 — 모델 바꿀 때마다 회귀 비교
5. **스키마 계약 §5 적용** — 특히 `level=lang → requirements null` 불변식

---

## 부록: 이번 세션에서 실제로 만든 구성요소

| 구성요소 | 역할 | 상태 |
|---|---|---|
| `guide_auto_analyze.py` | 폴더→섹션 라우터 (가이드→KB) | ✅ |
| `build_kb_smart.py` | 자격·택소노미 인덱스 | ✅ |
| `kb_quality_check.py` | 품질 리포트 | ✅ |
| `_validate_llmparse.py` | **원문 대조 검증** (§4 +0.2) | ✅ |
| `_assess_llmparse_trust.py` | **문서 등급 판정** (§2) | ✅ |
| `_merge_llm_into_kb.py` | 정본 병합 (fill-only, 출처태그) | ✅ |
| `_sync_consulting_db.py` | 파생 in-place 동기화 | ✅ |
| `_parse_real_pdfs.py` | 등급 라우팅 + OCR 폴백 + pro 호출규약 | ✅ |
| `daily_homepage_2027_check.py` | 홈페이지 일일 검수 | ✅ |
| 크론 `78d789b31dd0` | 평일 09:00 통합 | ✅ |
| 크론 `7dcc1a62eebf` | 변경감지 모니터 | ✅ |
| **문서 등급 라우터 (통합)** | §2 자동화 | ⏳ 신규 |
| **Golden Set** | §8 회귀 | ⏳ 신규 |
